program Volume
{	/*Using an unassigned variable in a combined assignment*/
	float r = 5, a, p, b = 7; 
	
	a = (3.14) * r * r; 
	p *=  3.14 * b;  
	/*Display the results*/
	PRINT ( "The output results are " , r, " ", a, " ", b);
}
