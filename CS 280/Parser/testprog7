PROGRAM Area
{
	/*Illegal operand type for a logical operation */
	float r = 4, a ; 
	bool tag1 = true, tag2;
		
	tag2 = tag1 && r ;
		
	/*Display the results*/
	PRINT ( "The output results are " , r, tag1, tag2);
}
